# Copy this file to .env and fill in your own values.
# Do NOT commit .env to your repository -- it contains credentials.

DB_ROOT_PASSWORD=changeme_root
DB_NAME=ticketdb
DB_USER=flaskuser
DB_PASSWORD=changeme_flask
